version: "3.3"

services:
  app:
    image: x11test:latest
    build: .
    tty: true
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev/video0:/dev/video0
    network_mode: host
